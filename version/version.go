package version

var (
	// VerMajor is the major version of tidb2snowflake
	VerMajor = 0
	// VerMinor is the minor version of tidb2snowflake
	VerMinor = 1
	// VerPatch is the patch version of tidb2snowflake
	VerPatch = 0
	// VerName is an alternative name of the version
	VerName = "tidb2snowflake"
	// GitHash is the current git commit hash
	GitHash = "Unknown"
	// GitRef is the current git reference name (branch or tag)
	GitRef = "Unknown"
)
